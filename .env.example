# Copy to .env and fill in. .env is gitignored; never commit real keys.
# Every variable in the README's Configuration table can be set here.
FESTIVAL_API_KEY=fes_your_key_here
TRIBLI_OPP_RSS_LIMIT=15
TRIBLI_FESTIVAL_CREDITS=6
